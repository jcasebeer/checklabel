#cloud-config
# First-boot provisioning for the label-check VPS (Ubuntu 22.04/24.04).
# Paste into your provider's "user data" field when creating the server,
# after replacing the SSH key placeholder.
#
# Result: a non-root deploy user with Docker, automatic security updates,
# and a firewall that denies ALL inbound traffic except SSH. The app itself
# is reached only through the Cloudflare Tunnel (outbound-only), so no web
# ports are ever opened.

# Key-only SSH, enforced the cloud-init-native way. ssh_pwauth makes
# cloud-init itself write PasswordAuthentication no; the 00- drop-in below
# additionally wins over any provider-written sshd_config.d file (sshd uses
# first-value-wins and includes the directory in lexical order, so editing
# the main sshd_config with sed is NOT reliable on Ubuntu cloud images).
ssh_pwauth: false
disable_root: true

write_files:
  - path: /etc/ssh/sshd_config.d/00-hardening.conf
    permissions: "0644"
    content: |
      PasswordAuthentication no
      KbdInteractiveAuthentication no
      PermitRootLogin no

users:
  - name: deploy
    groups: [docker]
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys:
      - ssh-rsa  dev@BLIPBLOP9000
      - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIDzPue8GXAX3MG1v29i7y5HSyuSkJ8hKQaa5AnYS3LAB jack@vandalay

package_update: true
package_upgrade: true
packages:
  - ufw
  - unattended-upgrades
  - rsync

# Docker from the official convenience script (installs compose plugin too).
runcmd:
  - curl -fsSL https://get.docker.com | sh
  - systemctl enable --now docker
  - mkdir -p /opt/label-check
  - chown deploy:deploy /opt/label-check
  # Firewall: deny everything inbound except SSH. No HTTP/HTTPS ports --
  # cloudflared connects outward to Cloudflare.
  - ufw default deny incoming
  - ufw default allow outgoing
  - ufw allow OpenSSH
  - ufw --force enable
  # Apply the SSH hardening drop-in written above.
  - systemctl restart ssh
  - dpkg-reconfigure -f noninteractive unattended-upgrades
