[pytest]
testpaths = tests
markers =
    integration: runs real model calls against TTB COLA label fixtures (needs ANTHROPIC_API_KEY and tests/fixtures/cola)
# Integration tests are opt-in: run them with  pytest -m integration
addopts = -m "not integration"
